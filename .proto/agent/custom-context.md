# Host agent context

Workspace setup phase gates and checkpoint lines are defined in **proto-plugin** (`bootstrap-setup-phase-gates.ts`) and emitted by the onboarding **Copy setup prompt** (`buildBootstrapSetupCopyText`).

Add host-specific conventions below this line only.
