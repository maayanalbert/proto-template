# Host-only — keep in `.env.local`.
SOURCE_PATH=/absolute/path/to/your/source-app

# Optional: comment + gallery persistence (Comments sidebar setup prompt)
KV_REST_API_URL=
KV_REST_API_TOKEN=
BLOB_READ_WRITE_TOKEN=

# Optional: PR split spec panel
# GITHUB_TOKEN=
